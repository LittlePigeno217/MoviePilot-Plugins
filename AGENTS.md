# Project Rules

## 115 Plugin Development

- When developing or modifying a 115 plugin feature, reference the corresponding 115 plugin implementation in [DDSRem-Dev/MoviePilot-Plugins](https://github.com/DDSRem-Dev/MoviePilot-Plugins).
- Every feature must map to an appropriate reference implementation from that repository. Preserve compatible behavior, interfaces, and edge-case handling unless a documented project-specific requirement calls for a difference.

## Plugin Debugging

- During plugin development, always install and debug the plugin in the MoviePilot Docker Compose project on the NAS connected through the local `.ssh` configuration.

## Repository Layout

The layout follows the official [MoviePilot-Plugins](https://github.com/jxxghp/MoviePilot-Plugins) repository.

- Plugin sources live in `plugins/<plugin-id-lowercase>/`, indexed by the root `package.json`. One implementation serves both V2 and V3, so each index entry declares `"v2": true` and `"v3": true`; there is no `plugins.v2/` or `plugins.v3/` split.
- Tests live in the root `tests/<plugin-id-lowercase>/`, never inside a plugin directory, and import plugins through the production namespace `app.plugins.<plugin_id>`.
- Vue federation output goes to `dist/assets/`; `get_render_mode()` returns `("vue", "dist/assets")`.
- `README.md` is generated by `scripts/generate_readme.py` from `package.json` — edit the script or the data, never the file.
- Release requirements and repository conventions: [docs/Repository_Guide.md](docs/Repository_Guide.md). Test conventions: [tests/README.md](tests/README.md).
