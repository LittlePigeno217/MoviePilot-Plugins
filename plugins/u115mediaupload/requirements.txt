# 115 媒体上传插件依赖

# 生产环境依赖
qrcode>=7.4.0  # 二维码生成库，用于 115 登录二维码
orjson>=3.9.0  # 高性能 JSON 序列化库（可选，如果缺失会自动降级到 json）
apscheduler>=3.10.0  # 任务调度库，用于定时增量上传（可选）

# 开发环境依赖
pytest>=7.0.0  # 单元测试框架
pytest-cov>=4.0.0  # 代码覆盖率工具（可选）
